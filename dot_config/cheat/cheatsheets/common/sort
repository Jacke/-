# To sort a file:
sort file

# To sort a file by keeping only unique:
sort -u file

# To sort a file and reverse the result:
sort -r file

# To sort a file randomly:
sort -R file

# To sort by human readable file size (e.g., 2K 1G)
sort -h

# To sort on the numerical n1th (between charecter 4 and 5) then n2th field separated by tabs 
sort -n -k n1.4,n1.5 -k n2,n2 -t "\t" -u 