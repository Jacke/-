
# Also show Header
Use -i
# Dump header to file
USe -D|--dump-header <filename>
# Make a POST Request
Use -X POST argument
# Add POST Data to a Request
Use -d var=val argument
# Construct a Query String
Use -G argument
# Add HTTP Headers
Use -H 'Header: Value' argument
# Change the User Agent
Use -A 'User Agent' argument
# Set Cookies
Use -b name=value argument
# Add a Referrer
Use -e URL argument
# Follow a 3XX Redirect
Use -L argument
# Use the Basic HTTP Authentication
Use -u user:pass argument
# Print the Response Headers
Use -i argument
# Use a Proxy
Use -x protocol://host:port argument
# Ignore the SSL Certificate
Use -k argument
# Make Curl Silent
Use -s argument
# Save the Response to a File
Use -o file argument
# Make Curl Slow
Use --limit-rate 8k (8KB/sec) argument
# Debug Curl Requests
Use -v or --trace arguments
# Make a GET Request
(No arguments required, it's the default)