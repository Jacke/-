####################################################################################################################################
##########################             MOTD                         ################################################################
####################################################################################################################################
if exists $MOTD; then
  if [[ -r "$HOME/Scripts/bin/cache_out" ]]; then
    eval "cache_out $MOTD" # Faster shell startup
  else
    eval "$MOTD $MOTD_PARAMS"
  fi
fi
